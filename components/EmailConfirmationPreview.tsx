// This component is deprecated and no longer used as of the transition to a real backend email verification system.
// The frontend now calls a signup API endpoint, and the backend is responsible for sending a real HTML email.
// This file is kept to prevent import errors but can be safely deleted if all references are removed.

const EmailConfirmationPreview = () => {
    return null;
};

export default EmailConfirmationPreview;