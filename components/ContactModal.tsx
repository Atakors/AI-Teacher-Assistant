import React from 'react';

// This component is disabled as the app now uses mailto: links for contact.
// It can be removed if all references are updated.
const ContactModal: React.FC<any> = () => {
  return null;
};

export default ContactModal;
